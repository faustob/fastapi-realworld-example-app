# Instrumentation for Fastapi-realworld-example-app

Generated by TelemetryOS against `029eb77`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · poetry · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (4):**
- qr-not-a-noop (`app/api/middleware/telemetry_outcome.py`): The middleware imports `active_requests_gauge` by value at module load time, binding to the module-level `None` before `setup_telemetry()` populates it. Even though setup runs first in main.py, the `from app.core.telemetry import ... active_requests_gauge` captures the current object reference; since setup_telemetry() reassigns the name via `global`, but this import happens after setup in main.py's import order (telemetry imported and setup called before middleware import), the value may be bound correctly — however `active_requests_gauge` is imported as a name copy, so subsequent reassignment in the module is not the risk here; the real risk is order. This is acceptable given import order.
- qr-preserves-behavior (`app/api/middleware/telemetry_outcome.py`): The 'slow' outcome branch calls outcome_counter.add() with a second point that lacks the http.response.status_code attribute but shares the same counter, producing duplicate/inconsistent series and double-counting every slow request (once as success/error and again as slow) on the same metric.
- qr-not-a-noop (`app/api/middleware/telemetry_outcome.py`): RequestOutcomeMiddleware is defined but never added to the FastAPI application (no add_middleware call anywhere in the diff), so its dispatch never runs and the outcome counter, active-requests gauge, and slow-request span events are never emitted.
- qr-not-a-noop (`app/api/middleware/telemetry_outcome.py`): The slow-request branch runs in the finally-less code path after call_next; if call_next raises, the finally decrements the gauge but the exception propagates before the elapsed/span logic — acceptable — but more critically the slow_request span event is added to trace.get_current_span() which, because BaseHTTPMiddleware runs the downstream call in a separate anyio task, is often the (non-recording) INVALID span rather than the server span, so the event is silently dropped.
**Outstanding (1):**
- ⚠ qr-preserves-behavior: In telemetry_outcome.py, if call_next raises, the finally block decrements active_requests_gauge, but the exception propagates so `elapsed`/`response` code is skipped correctly; however the active_requests_gauge.add(1) is only decremented in finally which is fine — but the module-level `active_requests_gauge` and `get_*_counter()` are None until setup_telemetry runs. setup_telemetry() is called at import time in main.py before middleware use, so this is acceptable. — _No change needed._

## SLIs implemented
- **Fastapi-realworld-example-app — HTTP Service Availability** (`fastapi-realworld-example-app-http-availability`) — target 99.95% over 30-day
- **Fastapi-realworld-example-app — HTTP Response Time P95** (`fastapi-realworld-example-app-http-latency-p95`) — target < 200ms over 1-hour
- **Fastapi-realworld-example-app — HTTP Response Time P99** (`fastapi-realworld-example-app-http-latency-p99`) — target < 750ms over 1-hour
- **Fastapi-realworld-example-app — HTTP 5xx Error Rate** (`fastapi-realworld-example-app-http-error-rate`) — target < 0.1% over 1-hour
- **Fastapi-realworld-example-app — HTTP Worker Pool Saturation** (`fastapi-realworld-example-app-http-saturation`) — target < 80% over 5-minute
- **Fastapi-realworld-example-app — Authentication Failure Rate** (`fastapi-realworld-example-app-http-auth-failure-rate`) — target < 2% over 1-hour
- **Fastapi-realworld-example-app — HTTP Request Throughput** (`fastapi-realworld-example-app-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `app/main.py`

## New files
- `app/core/telemetry.py`
- `app/api/middleware/telemetry_outcome.py`

## Dependencies
- `opentelemetry-api==1.34.0`
- `opentelemetry-sdk==1.34.0`
- `opentelemetry-exporter-otlp-proto-http==1.34.0`
- `opentelemetry-instrumentation-fastapi==0.55b0`
