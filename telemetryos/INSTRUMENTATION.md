# Instrumentation for Fastapi-realworld-example-app

Generated by TelemetryOS against `029eb77`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · poetry · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (3):**
- qr-not-a-noop (`app/api/dependencies/telemetry_middleware.py`): RequestOutcomeMiddleware is defined but never registered in app/main.py (no application.add_middleware(RequestOutcomeMiddleware) call), so none of its counters, active-request tracking, or slow-request events are ever emitted.
- qr-not-a-noop (`app/main.py`): _unhandled_exception_handler uses trace.StatusCode.ERROR, but StatusCode is not exposed on the opentelemetry.trace module namespace (the correct symbol is trace.Status/trace.StatusCode is defined in opentelemetry.trace.status). As written this raises AttributeError at runtime inside the exception handler, masking the original error.
- qr-not-a-noop (`app/core/telemetry.py`): When an OTel agent has already set the global TracerProvider, get_tracer_provider() returns a ProxyTracerProvider (or the SDK provider from another package), and the isinstance(..., TracerProvider) branch is skipped so no exporter/span processor is added; but under app-sdk with no agent the default is a ProxyTracerProvider, so a real TracerProvider is created and set correctly — no defect in the app-sdk path.
**Outstanding (3):**
- ⚠ qr-not-a-noop (`app/core/telemetry.py`:10): `time` is imported but never used; the `Optional`, `Dict`, `Callable` typing imports are also unused. These are compile-class lint issues the gate can address. — _Remove unused imports._
- ⚠ general (`app/api/dependencies/telemetry_middleware.py`:33): `request.scope.get("route")` yields the matched route only if routing has occurred; with BaseHTTPMiddleware the route is resolved downstream, so `route` is typically None here and route_template falls back to the raw URL path, producing high-cardinality label values on the metric counters. — _Use the matched route from response/scope after call_next, or accept the raw path only when no route matches, and be aware of cardinality._
- ⚠ qr-preserves-behavior (`app/api/dependencies/telemetry_middleware.py`:50): The `tenant` label is populated directly from the client-supplied `x-api-key` header, which is unbounded/attacker-controlled and creates unbounded metric time-series cardinality (a metrics cardinality explosion / potential DoS on the metrics pipeline). — _Do not use the raw API key value as a metric attribute; map it to a bounded identifier or omit it._

## SLIs implemented
- **Fastapi-realworld-example-app — HTTP Service Availability** (`fastapi-realworld-example-app-http-availability`) — target 99.95% over 30-day
- **Fastapi-realworld-example-app — HTTP Response Time P95** (`fastapi-realworld-example-app-http-latency-p95`) — target < 200ms over 1-hour
- **Fastapi-realworld-example-app — HTTP Response Time P99** (`fastapi-realworld-example-app-http-latency-p99`) — target < 750ms over 1-hour
- **Fastapi-realworld-example-app — HTTP 5xx Error Rate** (`fastapi-realworld-example-app-http-error-rate`) — target < 0.1% over 1-hour
- **Fastapi-realworld-example-app — HTTP Worker Pool Saturation** (`fastapi-realworld-example-app-http-saturation`) — target < 80% over 5-minute
- **Fastapi-realworld-example-app — Authentication Failure Rate** (`fastapi-realworld-example-app-http-auth-failure-rate`) — target < 2% over 1-hour
- **Fastapi-realworld-example-app — HTTP Request Throughput** (`fastapi-realworld-example-app-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `app/main.py`

## New files
- `app/core/telemetry.py`
- `app/api/dependencies/telemetry_middleware.py`

## Dependencies
- `opentelemetry-api==1.34.0`
- `opentelemetry-sdk==1.34.0`
- `opentelemetry-exporter-otlp==1.34.0`
- `opentelemetry-instrumentation-fastapi==0.55b0`
