# Instrumentation for Fastapi-realworld-example-app

Generated by TelemetryOS against `029eb77`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · poetry · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (5):**
- qr-preserves-behavior (`app/core/telemetry.py`): The diff registers a catch-all @app.exception_handler(Exception) that unconditionally returns a JSONResponse with status 500 and body {"error":"internal_error"}. This overrides the app's existing exception propagation/handling for all unhandled exceptions, changing response bodies and swallowing errors that previously would surface differently.
- general (`app/core/telemetry.py`): trace.set_status is called as span.set_status(trace.StatusCode.ERROR, ...), but StatusCode lives under opentelemetry.trace.StatusCode and set_status expects a Status object or a StatusCode; passing StatusCode.ERROR positionally with a description string is the correct 2-arg form only for Status. Using trace.StatusCode.ERROR here works but trace.Status is the idiomatic wrapper; verify the symbol resolves.
- qr-preserves-behavior (`app/core/telemetry.py`): The added http middleware iterates over request.app.routes and calls route.matches(request.scope) on every request in a dead loop that does nothing (only `pass`), adding per-request overhead on the hot path with no effect.
- qr-not-a-noop (`app/core/telemetry.py`): The tracer-provider "already registered" guard checks the SDK provider class name via `trace.get_tracer_provider().__class__.__name__ == "TracerProvider"`, but that condition is also true right after `trace.set_tracer_provider(tracer_provider)` succeeds. Since set_tracer_provider on the OTel API logs a warning and does NOT overwrite an already-set provider, if an agent registered first, this diff's provider silently fails to install and its BatchSpanProcessor/OTLP exporter never run — though this specific branch is defensive; the real defect is below.
- qr-preserves-behavior (`app/core/telemetry.py`): The added `@app.middleware("http")` reassigns `route_template` from `request.scope.get("route").path` inside the finally block, which is additive metric labeling and does not alter response flow; no confirmed behavior change.
**Outstanding (1):**
- ⛔ qr-not-a-noop (`app/core/telemetry.py`:37): The provider setup wraps set_tracer_provider/set_meter_provider in bare try/except that swallows all exceptions, but more importantly the middleware and instruments were created via get_meter/get_tracer which is fine; however if OTLPMetricExporter/OTLPSpanExporter construction raises, the except silently discards it and no SDK provider is registered, so create_meter/create_counter bind to the no-op API provider and emit nothing while appearing successful. — _Do not swallow provider-build failures; let them propagate (or log) so a failed SDK registration is visible rather than silently degrading to no-op instruments._

## SLIs implemented
- **Fastapi-realworld-example-app — HTTP Service Availability** (`fastapi-realworld-example-app-http-availability`) — target 99.95% over 30-day
- **Fastapi-realworld-example-app — HTTP Response Time P95** (`fastapi-realworld-example-app-http-latency-p95`) — target < 200ms over 1-hour
- **Fastapi-realworld-example-app — HTTP Response Time P99** (`fastapi-realworld-example-app-http-latency-p99`) — target < 750ms over 1-hour
- **Fastapi-realworld-example-app — HTTP 5xx Error Rate** (`fastapi-realworld-example-app-http-error-rate`) — target < 0.1% over 1-hour
- **Fastapi-realworld-example-app — HTTP Worker Pool Saturation** (`fastapi-realworld-example-app-http-saturation`) — target < 80% over 5-minute
- **Fastapi-realworld-example-app — Authentication Failure Rate** (`fastapi-realworld-example-app-http-auth-failure-rate`) — target < 2% over 1-hour
- **Fastapi-realworld-example-app — HTTP Request Throughput** (`fastapi-realworld-example-app-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `app/main.py`

## New files
- `app/core/telemetry.py`

## Dependencies
- `opentelemetry-api==1.34.0`
- `opentelemetry-sdk==1.34.0`
- `opentelemetry-exporter-otlp-proto-http==1.34.0`
- `opentelemetry-instrumentation-fastapi==0.55b0`
