# Instrumentation for Fastapi-realworld-example-app

Generated by TelemetryOS against `029eb77`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · poetry · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (6):**
- qr-preserves-behavior (`app/core/telemetry.py`): setup_telemetry registers a catch-all Exception handler via application.add_exception_handler(Exception, _unhandled_exception_handler), which intercepts previously-unhandled exceptions and returns a 500 JSONResponse instead of letting Starlette propagate them, changing the app's error-handling behavior and response bodies.
- qr-preserves-behavior (`app/main.py`): setup_telemetry(application) is called before add_middleware(SaturationMiddleware) and other middlewares. FastAPIInstrumentor/exception-handler and Starlette middleware ordering: middleware added later wraps earlier ones, so the SaturationMiddleware span-event logic runs outside the OTel FastAPI span, and trace.get_current_span() in dispatch may return a non-recording span before the instrumentation span is created.
- general (`app/core/telemetry.py`): trace.StatusCode is referenced in _unhandled_exception_handler, but StatusCode lives at opentelemetry.trace.StatusCode only via the trace module's re-export; set_status expects a Status/StatusCode — passing StatusCode plus a description string is the older signature and may be incorrect depending on API version.
- qr-preserves-behavior (`app/core/telemetry.py`): The tenant_request_counter records the raw Authorization header presence but the attribute cardinality is bounded; however request_outcome_counter includes http.response.status_code as an attribute which can create per-status-code cardinality — acceptable, but combined with unresolved route_template (falls back to request.url.path with raw path values) it produces unbounded high-cardinality metric attributes.
- qr-preserves-behavior (`app/core/telemetry.py`): In SaturationMiddleware.dispatch's exception branch, span.add_event("slow_request", {...}) references route_template BEFORE it is resolved from request.scope (that resolution block runs afterward), so the slow-request event always records route_template=None regardless of the actual route.
- qr-preserves-behavior (`app/main.py`): SaturationMiddleware is added before CORSMiddleware, so it wraps the outermost layer and its active-requests/outcome counting occurs outside CORS handling; combined with add_middleware ordering (last-added runs first) the saturation counter also sees CORS preflight responses. This alters request accounting semantics but is functional.
**Outstanding (2):**
- ⚠ qr-preserves-behavior (`app/main.py`:18): SaturationMiddleware is added via add_middleware BEFORE setup_telemetry runs FastAPIInstrumentor.instrument_app; because Starlette middleware executes in reverse order of registration relative to the OTel middleware, the slow_request span events and error status set on trace.get_current_span() may attach to a different (or no active) span than the FastAPI server span, undermining the span-event enrichment. — _Ensure FastAPIInstrumentor.instrument_app runs before adding SaturationMiddleware so the OTel server span is the current span while the middleware records events._
- ⚠ qr-not-a-noop (`pyproject/dependencies`:1): The proposed dependencies use opentelemetry-instrumentation-fastapi but it is not listed in the wiring recipe dependencies and no version pin is provided; FastAPIInstrumentor is imported in telemetry.py and will fail to import if the package is absent. — _Add opentelemetry-instrumentation-fastapi (pinned compatibly with 1.34) to the dependency set._

## SLIs implemented
- **Fastapi-realworld-example-app — HTTP Service Availability** (`fastapi-realworld-example-app-http-availability`) — target 99.95% over 30-day
- **Fastapi-realworld-example-app — HTTP Response Time P95** (`fastapi-realworld-example-app-http-latency-p95`) — target < 200ms over 1-hour
- **Fastapi-realworld-example-app — HTTP Response Time P99** (`fastapi-realworld-example-app-http-latency-p99`) — target < 750ms over 1-hour
- **Fastapi-realworld-example-app — HTTP 5xx Error Rate** (`fastapi-realworld-example-app-http-error-rate`) — target < 0.1% over 1-hour
- **Fastapi-realworld-example-app — HTTP Worker Pool Saturation** (`fastapi-realworld-example-app-http-saturation`) — target < 80% over 5-minute
- **Fastapi-realworld-example-app — Authentication Failure Rate** (`fastapi-realworld-example-app-http-auth-failure-rate`) — target < 2% over 1-hour
- **Fastapi-realworld-example-app — HTTP Request Throughput** (`fastapi-realworld-example-app-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `app/main.py`

## New files
- `app/core/telemetry.py`

## Dependencies
- `opentelemetry-api`
- `opentelemetry-sdk`
- `opentelemetry-exporter-otlp-proto-http`
- `opentelemetry-instrumentation-fastapi`
- `opentelemetry-api==1.34.0`
- `opentelemetry-sdk==1.34.0`
- `opentelemetry-exporter-otlp-proto-http==1.34.0`
