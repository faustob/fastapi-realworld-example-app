# Instrumentation for Fastapi-realworld-example-app

Generated by TelemetryOS against `029eb77`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · poetry · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (4):**
- qr-not-a-noop (`app/core/telemetry.py`): Module-level instruments (request_outcome_counter, ACTIVE_REQUESTS, and the others) are created from `_meter = metrics.get_meter("app")` at import time. In Python OTel there is no delegating proxy for instruments: get_meter() before set_meter_provider() returns a _ProxyMeter, and its instruments only relay to the real provider once one is set. But app/main.py imports these symbols at module load, which resolves the _ProxyMeter before setup_telemetry() runs; that is tolerated by the proxy — however the module-level docstring/comment claim they bind 'lazily against the GLOBAL meter provider' which is not the mechanism. The real defect is timing-independent only if the proxy relays; verify import order is fine.
- qr-not-a-noop (`app/core/telemetry.py`): setup_telemetry() calls _create_instruments(), but no function named _create_instruments is defined anywhere in the module; this raises NameError at startup and aborts application creation, so no telemetry is registered and the app fails to boot.
- qr-not-a-noop (`app/main.py`): main.py imports `active_requests_gauge` from app.core.telemetry, but that name refers to a function that just returns ACTIVE_REQUESTS and is unused; combined with the missing _create_instruments the import chain will fail at startup.
- qr-not-a-noop (`app/core/telemetry.py`): setup_telemetry() calls _create_instruments() but no function named _create_instruments is defined in the module; instruments are instead created at module top level. The undefined call raises NameError at startup, crashing get_application().
**Outstanding (1):**
- ⛔ qr-not-a-noop (`app/core/telemetry.py`:95): The module-level `_meter = metrics.get_meter("app")` and all instruments (ACTIVE_REQUESTS, request_outcome_counter, etc.) are created at import time, which happens before setup_telemetry() runs and registers the MeterProvider. In Python's OTel SDK, get_meter() at that point returns a meter bound to the default no-op/proxy provider snapshot, and once set_meter_provider() is later called these already-created instruments do not get rebound, so they emit nothing. — _Create the instruments inside setup_telemetry() (or lazily via get_meter() after registration) rather than at module import time, so they bind to the registered MeterProvider._

## SLIs implemented
- **Fastapi-realworld-example-app — HTTP Service Availability** (`fastapi-realworld-example-app-http-availability`) — target 99.95% over 30-day
- **Fastapi-realworld-example-app — HTTP Response Time P95** (`fastapi-realworld-example-app-http-latency-p95`) — target < 200ms over 1-hour
- **Fastapi-realworld-example-app — HTTP Response Time P99** (`fastapi-realworld-example-app-http-latency-p99`) — target < 750ms over 1-hour
- **Fastapi-realworld-example-app — HTTP 5xx Error Rate** (`fastapi-realworld-example-app-http-error-rate`) — target < 0.1% over 1-hour
- **Fastapi-realworld-example-app — HTTP Worker Pool Saturation** (`fastapi-realworld-example-app-http-saturation`) — target < 80% over 5-minute
- **Fastapi-realworld-example-app — Authentication Failure Rate** (`fastapi-realworld-example-app-http-auth-failure-rate`) — target < 2% over 1-hour
- **Fastapi-realworld-example-app — HTTP Request Throughput** (`fastapi-realworld-example-app-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `app/main.py`

## New files
- `app/core/telemetry.py`

## Dependencies
- `opentelemetry-api==1.34.0`
- `opentelemetry-sdk==1.34.0`
- `opentelemetry-exporter-otlp-proto-http==1.34.0`
- `opentelemetry-instrumentation-fastapi>=0.55b0`
