# Instrumentation for Primary Flow — End-to-End

Generated by TelemetryOS against `029eb77`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · poetry · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (3):**
- qr-duplicate-file-path (`app/core/flow_middleware.py`): "app/core/flow_middleware.py" appears 2× in the changeset (new file + new file) — two conflicting full contents for one path. Only the last-written wins, and it may drop declarations/imports the other version added, so the file won't compile.
- qr-preserves-behavior (`app/core/flow_middleware.py`): The flow_middleware.py content is truncated/empty in the diff — the dispatch method is cut off mid-expression and never completes, so PrimaryFlowMiddleware cannot be defined and app/main.py's import of it will fail at import time.
- qr-metric-naming (`app/core/flow_metrics.py`): The histogram measures a duration in seconds but is named flow.duration without a unit suffix, while the counters use a .total suffix; per semconv style a seconds histogram should carry its unit (e.g. flow.duration.seconds or use the semconv http.server.request.duration for inbound HTTP request duration).
**Outstanding (3):**
- ⚠ qr-preserves-behavior (`app/main.py`:39): FastAPIInstrumentor.instrument_app(application) and the BaseHTTPMiddleware-based PrimaryFlowMiddleware both wrap the same request path; combined with the flow span this produces redundant nested spans, but the bigger risk is the broad try/except Exception: pass silently swallowing all instrumentation failures making misconfiguration invisible. — _Narrow the exception handling or let import errors surface; ensure the FastAPI instrumentor and custom middleware do not duplicate root spans._
- ⚠ qr-metric-naming (`app/core/flow_metrics.py`:18): New count instruments carry a redundant '.total' suffix (flow.entries.total, flow.outcomes.total, flow.validation.outcomes.total); per semconv style plain counts are unsuffixed, so the '.total' encodes an implementation/aggregation detail. — _Drop the '.total' suffix (e.g. flow.entries, flow.outcomes, flow.validation.outcomes) to match semconv counter naming._
- ⚠ qr-metric-naming (`app/core/flow_metrics.py`:30): flow.duration.seconds measures inbound HTTP request duration and would be better expressed with the OTel semantic-convention name http.server.request.duration; the custom name reinvents a convention that exactly matches the domain. — _Prefer http.server.request.duration (unit s) for inbound request duration, or keep a domain name but align with semconv._

## SLIs implemented
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour

## Modified files
- `app/main.py`

## New files
- `app/core/telemetry.py`
- `app/core/flow_metrics.py`
- `app/core/flow_middleware.py`

## Dependencies
No additional dependencies identified beyond your existing OpenTelemetry setup.
