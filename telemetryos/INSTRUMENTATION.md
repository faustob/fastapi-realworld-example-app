# Instrumentation for Fastapi-realworld-example-app

Generated by TelemetryOS against `029eb77`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · poetry · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (4):**
- qr-endpoint-env-driven (`app/core/telemetry.py`): Hardcoded OTLP endpoint "http://localhost:4317" — wrong in every real deployment.
- qr-preserves-behavior (`app/core/telemetry.py`): In _active_requests_middleware, if call_next raises, the `finally` block runs but `response` is never assigned, and execution then continues to `status_code = response.status_code`, raising UnboundLocalError. This swallows/masks the original exception and changes error-handling behavior for any request that raises.
- general (`app/core/telemetry.py`): trace.StatusCode is not a valid attribute; the OpenTelemetry API exposes the enum as trace.Status/StatusCode via opentelemetry.trace.status, and set_status expects a Status object or a StatusCode from that module. `trace.StatusCode.ERROR` will raise AttributeError at runtime when a 5xx response occurs.
- qr-preserves-behavior (`app/core/telemetry.py`): instrument_app registers FastAPIInstrumentor and then app.middleware("http") is called AFTER FastAPI application startup wiring; app.middleware() adds middleware after include_router which is fine, but calling app.middleware after the app is fully constructed is acceptable — the real issue is _active_requests_middleware runs outside the FastAPIInstrumentor span for outcome mapping only if middleware order differs. This is not a confirmed defect.
**Outstanding (1):**
- ⚠ qr-preserves-behavior (`app/core/telemetry.py`:135): instrument_app registers FastAPIInstrumentor before adding the custom _active_requests_middleware, and app.middleware("http") is called after the app has already been created and routers included; while functional, mixing FastAPIInstrumentor spans with a manual outcome middleware duplicates HTTP metrics semantics but does not alter control flow, so this is only a minor concern. — _Consider consolidating HTTP metrics into a single source to avoid double-counting semantics._

## SLIs implemented
- **Fastapi-realworld-example-app — HTTP Service Availability** (`fastapi-realworld-example-app-http-availability`) — target 99.95% over 30-day
- **Fastapi-realworld-example-app — HTTP Response Time P95** (`fastapi-realworld-example-app-http-latency-p95`) — target < 200ms over 1-hour
- **Fastapi-realworld-example-app — HTTP Response Time P99** (`fastapi-realworld-example-app-http-latency-p99`) — target < 750ms over 1-hour
- **Fastapi-realworld-example-app — HTTP 5xx Error Rate** (`fastapi-realworld-example-app-http-error-rate`) — target < 0.1% over 1-hour
- **Fastapi-realworld-example-app — HTTP Worker Pool Saturation** (`fastapi-realworld-example-app-http-saturation`) — target < 80% over 5-minute
- **Fastapi-realworld-example-app — Authentication Failure Rate** (`fastapi-realworld-example-app-http-auth-failure-rate`) — target < 2% over 1-hour
- **Fastapi-realworld-example-app — HTTP Request Throughput** (`fastapi-realworld-example-app-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `app/main.py`

## New files
- `app/core/telemetry.py`

## Dependencies
- `opentelemetry-api`
- `opentelemetry-sdk`
- `opentelemetry-exporter-otlp`
- `opentelemetry-instrumentation-fastapi`
