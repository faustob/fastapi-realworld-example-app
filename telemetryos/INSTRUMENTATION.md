# Instrumentation for Fastapi-realworld-example-app

Generated by TelemetryOS against `029eb77`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · poetry · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (4):**
- qr-not-a-noop (`app/telemetry.py`): Module-level singletons `tracer = trace.get_tracer(__name__)` and `meter = metrics.get_meter(__name__)` are resolved at import time. All the instruments (http_requests_total, http_active_requests, auth_attempts_total, flow_* histograms/counters) are created from `meter` at module import. Since the diff never shows that `AppSettings.configure_telemetry()` is called BEFORE app/telemetry.py is imported, these instruments can bind to the global no-op MeterProvider and silently emit nothing. The wiring recipe requires the SDK be registered ONCE at startup before get_meter/get_tracer are used; nothing in the provided diff establishes that configure_telemetry() actually runs (no call site shown in main.py), so the instruments risk being permanent no-ops.
- qr-not-a-noop (`app/core/settings/app.py`): configure_telemetry() builds and registers the TracerProvider/MeterProvider, but the diff never shows it being called anywhere (no edit to main.py or startup event). Under app-sdk wiring, if nothing invokes configure_telemetry(), the global providers remain no-ops and all custom instrumentation emits nothing.
- general (`app/middleware/telemetry.py`): `trace.StatusCode` does not exist in the OpenTelemetry Python API; the status enum is `opentelemetry.trace.StatusCode` accessed via `trace.StatusCode`? Actually the correct type is `Status`/`StatusCode` from opentelemetry.trace.status, and set_status expects a Status object. `span.set_status(trace.StatusCode.ERROR, ...)` will raise AttributeError/incorrect usage at runtime. The two-arg form with a StatusCode is deprecated/invalid depending on version.
- qr-preserves-behavior (`app/middleware/telemetry.py`): http_active_requests is decremented twice on the success path: once in the `finally` block? No — decrement is not in finally. On the exception path it decrements once (in except) then re-raises. On the success path it decrements once after the try. But if response is obtained and then set_status/logic runs fine, that's one decrement. However on exception the except block decrements AND the code after the try/finally (`http_active_requests.add(-1, active_attrs)`) is never reached because raise propagates — OK. The real bug: the unconditional `http_active_requests.add(-1)` after the try is only reached on success, but there is also no decrement path if call_next succeeds and then a later line throws. More critically, the up-down counter can be double-counted/desynced because the decrement logic is split between except and post-try rather than the finally block, making the saturation gauge unreliable.
**Outstanding (5):**
- ⛔ qr-new-file-no-overwrite (`app/api/dependencies/authentication.py`): New file "app/api/dependencies/authentication.py" already exists in the repo — emitting it as a new file overwrites the original wholesale. — _Modify the existing file with additive fileEdits instead of recreating it._
- ⛔ qr-otel-api-signatures (`app/middleware/telemetry.py`:35): Counter.add() must be non-negative; use an UpDownCounter for decrements. — _Use create_up_down_counter() for values that can decrease._
- ⛔ qr-preserves-behavior (`app/middleware/telemetry.py`:30): When call_next raises an exception, the finally block records the request and decrements the active counter, but then execution continues past the try/finally to `record_http_request(...)` and `return response`. Since `response` was never assigned (the exception propagated out of the try), the code after the try block will not actually run — the exception re-propagates from the finally. However, if call_next raises, `status_code` stays 500 and the exception is correctly re-raised (behavior preserved for the error case). The real behavioral bug: on the success path the code works, but there is no `except` to re-raise; more importantly `record_http_request` and the return are unreachable when an exception occurs, meaning metrics for failed requests recording relies on that unreachable code — 5xx exceptions are never counted via record_http_request. This silently changes/omits telemetry for the exception path and the unreferenced `response` on error would be a NameError if reached. — _Record metrics and set span status inside the finally block (or via try/except/else), ensuring the request is recorded and re-raise the exception explicitly so failed requests are both counted and propagate unchanged._
- ⚠ qr-not-a-noop (`app/core/settings/app.py`:66): configure_telemetry() defines the SDK bootstrap that registers the global TracerProvider/MeterProvider, but nothing in the provided diff calls it (app/main.py is not shown and app/api/dependencies/authentication.py is empty). If configure_telemetry() is never invoked at process startup before the lazy instrument getters run, get_meter/get_tracer resolve to no-op providers and no telemetry is emitted. — _Ensure configure_telemetry() is invoked once at process startup (e.g., in app/main.py) before any request is served, and confirm that wiring is present in the diff._
- ⚠ qr-preserves-behavior (`app/telemetry.py`:220): record_http_request calls span.add_event on the current span without checking span.is_recording()/validity. If no span is active, trace.get_current_span() returns a non-recording default span; add_event is a safe no-op, so acceptable — but combined with the middleware always calling record_http_request even for non-instrumented requests, this adds work on the hot path. Minor, but the slow_request event will never be attached to a real span unless auto tracing middleware is installed, so the P99 triage feature is effectively inert. — _Guard span operations with span.is_recording() and ensure a tracing middleware/span exists, or emit the slow-request signal as a metric rather than a span event._

## SLIs implemented
- **Fastapi-realworld-example-app — HTTP Service Availability** (`fastapi-realworld-example-app-http-availability`) — target 99.95% over 30-day
- **Fastapi-realworld-example-app — HTTP Response Time P95** (`fastapi-realworld-example-app-http-latency-p95`) — target < 200ms over 1-hour
- **Fastapi-realworld-example-app — HTTP Response Time P99** (`fastapi-realworld-example-app-http-latency-p99`) — target < 750ms over 1-hour
- **Fastapi-realworld-example-app — HTTP 5xx Error Rate** (`fastapi-realworld-example-app-http-error-rate`) — target < 0.1% over 1-hour
- **Fastapi-realworld-example-app — HTTP Worker Pool Saturation** (`fastapi-realworld-example-app-http-saturation`) — target < 80% over 5-minute
- **Fastapi-realworld-example-app — Authentication Failure Rate** (`fastapi-realworld-example-app-http-auth-failure-rate`) — target < 2% over 1-hour
- **Fastapi-realworld-example-app — HTTP Request Throughput** (`fastapi-realworld-example-app-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `app/core/settings/app.py`

## New files
- `app/telemetry.py`
- `app/middleware/telemetry.py`
- `app/api/dependencies/authentication.py`

## Dependencies
- `opentelemetry-api`
- `opentelemetry-sdk`
- `opentelemetry-exporter-otlp`
