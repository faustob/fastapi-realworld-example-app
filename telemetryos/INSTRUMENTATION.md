# Instrumentation for Fastapi-realworld-example-app

Generated by TelemetryOS against `029eb77`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · poetry · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (7):**
- qr-endpoint-env-driven (`app/core/telemetry.py`): Hardcoded OTLP endpoint "http://localhost:4318" — wrong in every real deployment.
- qr-not-a-noop (`app/core/telemetry.py`): trace.set_tracer_provider / metrics.set_meter_provider do NOT raise when a provider is already registered; they log a warning and silently keep the FIRST provider. The try/except wrappers are dead code, but more importantly the comment/logic assumes an exception path that never fires — this is only a semantic issue, not a no-op, so this is not the defect.
- qr-not-a-noop (`app/core/telemetry.py`): The custom request_rate_counter emits a per-request attribute 'tenant' taken directly from the X-API-Key header, producing unbounded/high-cardinality metric label values that will explode the metrics timeseries and can OOM the SDK/collector.
- qr-preserves-behavior (`app/core/telemetry.py`): When call_next raises, the middleware's finally decrements _active_requests but the exception propagates before the counter/histogram code, so on error paths the request-outcome and request-rate counters are never recorded; also 'response' would be unbound if an exception occurred — though the exception exits first, the outcome metrics silently miss all failure cases.
- qr-endpoint-env-driven (`app/core/telemetry.py`): Hardcoded OTLP endpoint "http://localhost:4318" — wrong in every real deployment.
- qr-preserves-behavior (`app/core/telemetry.py`): When call_next raises, the finally block emits metrics but the middleware's final `return response` returns None after the exception is re-raised — actually the re-raise propagates, but if an exception is raised the code adds the outcome counter using response.status_code only in the else branch, which is correct; however the observable gauge callback reads a module-global `_active_requests` that is mutated non-atomically across concurrent requests, causing incorrect in-flight counts under concurrency.
- qr-preserves-behavior (`app/core/telemetry.py`): setup_telemetry wraps trace.set_tracer_provider / metrics.set_meter_provider in try/except expecting an exception if a provider is already registered, but the OTel API only logs a warning and does NOT raise on re-registration, so the 'tolerate pre-registered provider' comment is misleading and the except blocks are dead code (harmless but not functional).
**Outstanding (1):**
- ⚠ qr-preserves-behavior (`app/core/telemetry.py`:163): The `@app.middleware("http")` decorator is registered inside setup_telemetry, which is called before other add_middleware/CORS calls in get_application; Starlette applies middleware in reverse registration order, so adding this middleware before CORSMiddleware changes the effective middleware stack ordering relative to the original app assembly. — _Register the slow-request middleware after the existing middleware stack is configured, or document/verify the ordering does not change CORS behavior._

## SLIs implemented
- **Fastapi-realworld-example-app — HTTP Service Availability** (`fastapi-realworld-example-app-http-availability`) — target 99.95% over 30-day
- **Fastapi-realworld-example-app — HTTP Response Time P95** (`fastapi-realworld-example-app-http-latency-p95`) — target < 200ms over 1-hour
- **Fastapi-realworld-example-app — HTTP Response Time P99** (`fastapi-realworld-example-app-http-latency-p99`) — target < 750ms over 1-hour
- **Fastapi-realworld-example-app — HTTP 5xx Error Rate** (`fastapi-realworld-example-app-http-error-rate`) — target < 0.1% over 1-hour
- **Fastapi-realworld-example-app — HTTP Worker Pool Saturation** (`fastapi-realworld-example-app-http-saturation`) — target < 80% over 5-minute
- **Fastapi-realworld-example-app — Authentication Failure Rate** (`fastapi-realworld-example-app-http-auth-failure-rate`) — target < 2% over 1-hour
- **Fastapi-realworld-example-app — HTTP Request Throughput** (`fastapi-realworld-example-app-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `app/main.py`

## New files
- `app/core/telemetry.py`

## Dependencies
- `opentelemetry-api==1.34.1`
- `opentelemetry-sdk==1.34.1`
- `opentelemetry-exporter-otlp==1.34.1`
- `opentelemetry-instrumentation-fastapi==0.55b1`
