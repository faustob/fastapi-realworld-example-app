# Instrumentation for Fastapi-realworld-example-app

Generated by TelemetryOS against `029eb77`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · poetry · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (2):**
- qr-preserves-behavior (`app/main.py`): Registering an @application.exception_handler(Exception) that re-raises changes existing error-handling behavior: previously unhandled exceptions bubbled through Starlette's default ServerErrorMiddleware to produce a 500 response, but a registered handler that raises instead of returning a Response causes Starlette to propagate the exception differently and can break the intended 500 error flow. The handler also overrides any pre-existing generic exception handling.
- qr-preserves-behavior (`app/main.py`): trace.StatusCode is not a valid attribute of the opentelemetry.trace module; the status enum is opentelemetry.trace.StatusCode is actually exposed as trace.StatusCode only via import — set_status expects a Status/StatusCode from opentelemetry.trace.status, and StatusCode is available as trace.StatusCode, but Span.set_status's second positional argument is deprecated for description, so passing str(exc) positionally may not attach the message.
**Outstanding (2):**
- ⚠ qr-not-a-noop (`app/main.py`:12): main.py imports and calls setup_telemetry() from app.core.telemetry, but that module is not part of this diff and no registration file exists in the repo (existingRegistrationFiles is empty). If app/core/telemetry.py does not exist or does not actually set the global TracerProvider, no SDK is registered and all spans resolve to no-op. — _Include app/core/telemetry.py in this changeset with setup_telemetry() building a TracerProvider with an OTLP exporter and calling trace.set_tracer_provider(...)._
- ⚠ qr-preserves-behavior (`app/main.py`:4): JSONResponse is imported but never used, which will fail lint/strict checks; it appears the exception handler was refactored into middleware but the unused import was left behind. — _Remove the unused `from fastapi.responses import JSONResponse` import._

## SLIs implemented
- **Fastapi-realworld-example-app — HTTP Service Availability** (`fastapi-realworld-example-app-http-availability`) — target 99.95% over 30-day
- **Fastapi-realworld-example-app — HTTP Response Time P95** (`fastapi-realworld-example-app-http-latency-p95`) — target < 200ms over 1-hour
- **Fastapi-realworld-example-app — HTTP Response Time P99** (`fastapi-realworld-example-app-http-latency-p99`) — target < 750ms over 1-hour
- **Fastapi-realworld-example-app — HTTP 5xx Error Rate** (`fastapi-realworld-example-app-http-error-rate`) — target < 0.1% over 1-hour
- **Fastapi-realworld-example-app — HTTP Worker Pool Saturation** (`fastapi-realworld-example-app-http-saturation`) — target < 80% over 5-minute
- **Fastapi-realworld-example-app — Authentication Failure Rate** (`fastapi-realworld-example-app-http-auth-failure-rate`) — target < 2% over 1-hour
- **Fastapi-realworld-example-app — HTTP Request Throughput** (`fastapi-realworld-example-app-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `app/main.py`

## Dependencies
No additional dependencies identified beyond your existing OpenTelemetry setup.
