# Instrumentation for Primary Flow — End-to-End

Generated by TelemetryOS against `029eb77`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · poetry · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 1 generation pass — ⚠ passed with warnings.
**Outstanding (2):**
- ⚠ qr-export-pipeline (`app/core/telemetry.py`:21): The import line for BatchSpanProcessor ends with a literal backslash-n sequence (`export import BatchSpanProcessor\n`) instead of a real newline, producing an invalid import statement. — _Replace the literal `\n` with an actual newline so the import reads `from opentelemetry.sdk.trace.export import BatchSpanProcessor`._
- ⚠ qr-metric-naming (`app/core/telemetry.py`:63): The histogram `flow.duration` records seconds but its name lacks the recorded-unit suffix required by semconv style for durations. — _Rename to `flow.duration.seconds` (or `flow.duration` measured per semconv http.server.request.duration convention) so the name carries its unit._

## SLIs implemented
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour

## Modified files
- `app/main.py`

## New files
- `app/core/telemetry.py`
- `app/core/flow_middleware.py`

## Dependencies
- `opentelemetry-api`
- `opentelemetry-sdk`
- `opentelemetry-exporter-otlp-proto-http`
- `opentelemetry-instrumentation-fastapi`
