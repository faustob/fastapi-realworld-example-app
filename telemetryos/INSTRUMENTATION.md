# Instrumentation for Fastapi-realworld-example-app

Generated by TelemetryOS against `029eb77`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · poetry · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (2):**
- qr-not-a-noop (`app/core/telemetry.py`): The module-level instruments (auth_attempts_counter, http_active_requests, flow_*_counter/histogram, etc.) are created from get_meter() at import time, but app/main.py imports app.core.telemetry (triggering these creations) before it calls setup_telemetry(); actually setup_telemetry() is called at the same import so ordering matters. Since the module top-level _meter = get_meter() and instrument creations execute during the import of telemetry.py, and setup_telemetry() only runs after the whole module body finishes executing, every instrument binds to the default no-op MeterProvider and none of the custom metrics are ever exported.
- qr-preserves-behavior (`app/core/telemetry.py`): On repeated calls (e.g. test reload), the defensive try/except does not prevent re-registering providers because OTel's set_tracer_provider/set_meter_provider only logs a warning and keeps the first provider rather than raising; the _TELEMETRY_INITIALIZED guard covers the same-process case, but the comment claiming it is 'safe when an agent has already registered' via exception handling is inaccurate since set_*_provider does not raise.
**Outstanding (1):**
- ⚠ qr-not-a-noop (`pyproject.toml`): The FastAPI instrumentation import (opentelemetry.instrumentation.fastapi.FastAPIInstrumentor) is used but opentelemetry-instrumentation-fastapi==0.55b0 is the only instrumentation dep; instrument_fastapi_app swallows any ImportError, so if the package is missing FastAPI instrumentation silently emits nothing. The dependency is declared, so this is only a risk if the pin resolves incorrectly. — _Ensure opentelemetry-instrumentation-fastapi is installed and compatible with API 1.34; avoid swallowing ImportError so a missing dep is not silent._

## SLIs implemented
- **Fastapi-realworld-example-app — HTTP Service Availability** (`fastapi-realworld-example-app-http-availability`) — target 99.95% over 30-day
- **Fastapi-realworld-example-app — HTTP Response Time P95** (`fastapi-realworld-example-app-http-latency-p95`) — target < 200ms over 1-hour
- **Fastapi-realworld-example-app — HTTP Response Time P99** (`fastapi-realworld-example-app-http-latency-p99`) — target < 750ms over 1-hour
- **Fastapi-realworld-example-app — HTTP 5xx Error Rate** (`fastapi-realworld-example-app-http-error-rate`) — target < 0.1% over 1-hour
- **Fastapi-realworld-example-app — HTTP Worker Pool Saturation** (`fastapi-realworld-example-app-http-saturation`) — target < 80% over 5-minute
- **Fastapi-realworld-example-app — Authentication Failure Rate** (`fastapi-realworld-example-app-http-auth-failure-rate`) — target < 2% over 1-hour
- **Fastapi-realworld-example-app — HTTP Request Throughput** (`fastapi-realworld-example-app-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `app/main.py`

## New files
- `app/core/telemetry.py`

## Dependencies
- `opentelemetry-api==1.34.0`
- `opentelemetry-sdk==1.34.0`
- `opentelemetry-exporter-otlp-proto-http==1.34.0`
- `opentelemetry-instrumentation-fastapi==0.55b0`
