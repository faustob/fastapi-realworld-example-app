# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity primary-flow-end-to-end start -->
## Primary Flow — End-to-End

Generated by HoundOS against `029eb77`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · poetry · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 1 generation pass — ⚠ passed with warnings.
**Outstanding (2):**
- ⚠ qr-otel-version-pin (`pyproject.toml`:24): opentelemetry-api is pinned at 1.27.0, but this job targets the OTel 1.44 line (default-latest). Different PRs against one repo pinning different OTel lines makes the dependency move for no stated reason. — _Pin the OTel dependencies to the 1.44 line, the version this job resolved from the knowledge base._
- ⚠ sli-not-implemented (`app/api/routes/articles/articles_resource.py`:63): The flow throughput SLI (flow-primary-flow-e2e-flow-throughput) is backed by flow_entries_counter, but that counter is only incremented in the register() handler, not at the article-publish step. The primary business flow throughput at its terminal publish step has no entry counter increment, so publish-throughput cannot be measured. This is a partial coverage gap for the throughput SLI.

## SLIs implemented
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour

## Modified files
- `pyproject.toml`
- `app/main.py`
- `app/api/routes/authentication.py`
- `app/api/routes/articles/articles_resource.py`
- `app/api/errors/validation_error.py`

## New files
- `app/core/telemetry.py`

## Dependencies
- `opentelemetry-api ^1.27.0`
- `opentelemetry-sdk ^1.27.0`
- `opentelemetry-exporter-otlp-proto-http ^1.27.0`
- `opentelemetry-instrumentation-fastapi ^0.48b0`
<!-- houndos:entity primary-flow-end-to-end end -->
