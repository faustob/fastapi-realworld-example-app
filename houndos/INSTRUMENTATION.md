# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity fastapi-realworld-example-app start -->
## Fastapi-realworld-example-app

Generated by HoundOS against `029eb77`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · poetry · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 6 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
<details><summary>22 issues found and fixed during self-review</summary>

- qr-export-pipeline (`app/core/telemetry.py`): Neither the TracerProvider nor the MeterProvider is ever flushed or shut down at process exit; there is no shutdown/force_flush registered on the FastAPI shutdown event, so BatchSpanProcessor and PeriodicExportingMetricReader buffers are lost when the container stops.
- sli-not-implemented (`app/core/telemetry.py`): The HTTP latency P95 (fastapi-realworld-example-app-http-latency-p95) and P99 (fastapi-realworld-example-app-http-latency-p99) SLIs have no duration histogram measurement point; the middleware computes `duration` but only records it as a span event above a 750ms threshold, never into a latency instrument, so no P95/P99 can be derived.
- sli-not-implemented (`app/core/telemetry.py`): The E2E flow latency P95 (flow-primary-flow-e2e-flow-latency) and flow freshness (flow-primary-flow-e2e-flow-freshness) SLIs have no measurement point: only flow entry and terminal-outcome counters exist, with no timing that correlates a registration entry to its publish terminal, so no flow duration can be computed.
- qr-metric-naming (`app/core/telemetry.py`): The new metric names http.server.requests and flow.outcomes/flow.entry/auth.attempts mix conventions inconsistently: http.server.requests collides conceptually with the semconv http.server.request.* namespace but is a plural count, while the flow.* names are ad-hoc; no single domain-prefix style is established.
- general (`app/core/telemetry.py`): _worker_pool_busy_callback references the module-level name _active_request_count, which is never defined anywhere in the module; the observable gauge callback will raise NameError each collection cycle.
- sli-not-implemented (`app/core/telemetry.py`): The E2E flow latency P95 (flow-primary-flow-e2e-flow-latency), flow freshness (flow-primary-flow-e2e-flow-freshness) SLIs have no working measurement point: flow_duration histogram is created but never recorded to, and _flow_entry_timestamps is populated nowhere, so entry-to-terminal duration is never measured.
- qr-unwired-telemetry (`app/core/telemetry.py`): record_validation_step is a newly-defined exported helper that is never called from any code path in this diff, and existingHelperCallSites is empty, so the flow validation-step counter never records — the flow-primary-flow-e2e-validation-failure-rate SLI is unwired.
- sli-not-implemented (`app/core/telemetry.py`): The HTTP worker pool saturation SLI (fastapi-realworld-example-app-http-saturation) relies on http.server.worker_pool.busy, whose callback references an undefined symbol and will fail at collection, so the saturation metric cannot be computed.
- qr-preserves-behavior (`app/core/telemetry.py`): telemetry_middleware sets status_code=500 and its except branch tags the current span with error.type/ERROR status, but HTTPException raised inside the app is normally converted to a proper HTTP response by Starlette's ServerErrorMiddleware/exception handlers running OUTSIDE the middleware; the middleware's exception path only runs for exceptions that propagate past those, so this is acceptable, but the auth/flow attribution based on status_code=500 in the finally block will mislabel any request whose response propagated an exception.
- qr-metric-naming (`app/core/telemetry.py`): The counter names auth.attempts.count, flow.entry.count, flow.outcome.count and flow.validation.step.count append a '.count' suffix; per semconv style plain counts are unsuffixed, so these should drop the trailing .count segment.
- general (`app/core/telemetry.py`): Flow duration and completion are keyed by the raw Authorization header as user_id; the registration POST /users response's Authorization header (the client's token, if any) will differ from the article-publish request's token, so _flow_entry_timestamps lookups will rarely match and flow.duration/freshness will seldom be recorded.
- general (`app/core/telemetry.py`): FastAPIInstrumentor().instrument_app is called with exclude_spans=["receive","send"]; the accepted values are ASGI message stages 'receive'/'send' but the correct behavior/version must be verified, otherwise instrument_app may raise at startup and disable all HTTP tracing/duration spans backing the latency and availability SLIs.
- qr-not-a-noop (`app/core/telemetry.py`): _tracer_provider_ref and _meter_provider_ref are annotated as TracerProvider/MeterProvider but initialized to None, which is a type mismatch; harmless at runtime but flagged for the compile gate.
- qr-preserves-behavior (`app/main.py`): setup_telemetry(application) calls FastAPIInstrumentor().instrument_app(app) AND the diff also adds a custom telemetry_middleware. FastAPIInstrumentor already emits http.server.request.duration and request metrics/spans, so both layers record overlapping HTTP telemetry, producing duplicated request duration/count metrics.
- sli-not-implemented (`app/core/telemetry.py`): The HTTP Worker Pool Saturation SLI is backed only by http.server.worker_pool.size reported from the WEB_CONCURRENCY env var; there is no measurement of in-use/busy workers, so utilisation (busy/total) cannot be computed and saturation >80% cannot be evaluated.
- sli-not-implemented (`app/core/telemetry.py`): The E2E Business Flow Latency P95, Flow Throughput, and Flow Completion Freshness SLIs have no measurement point: only flow_entry_counter and flow_outcome_counter (plain counts) are recorded, with no flow-duration histogram or entry-to-terminal timing instrument.
- sli-not-implemented (`app/core/telemetry.py`): The Flow Validation Failure Rate SLI has no measurement point; no instrument records validation-check outcomes for the registration-to-publish flow.
- general (`app/core/telemetry.py`): The module source contains a literal backslash-n sequence (`_flow_start_times: dict = {}\n`) instead of an actual newline, which is a syntax error preventing the module from importing.
- qr-not-a-noop (`app/core/telemetry.py`): `_worker_pool_busy_callback` and `_authenticated_user_key`/middleware reference `_active_request_count` before it is defined at module scope (defined at line ~163, after the observable gauge callback is registered). While Python resolves this at call time, the gauge callback closes over the module global correctly; however the ordering is fragile and the busy gauge always reports process-local in-flight counts, not worker-pool busy workers.
- qr-preserves-behavior (`app/core/telemetry.py`): `shutdown_telemetry` is an async coroutine registered as a FastAPI 'shutdown' event handler, but it calls the SDK's synchronous blocking `force_flush()`/`shutdown()` inside the event loop, blocking the loop; more importantly the middleware's `flow_duration_histogram.record` and other work run without exception guards on OTLP export, which is acceptable, but the blocking flush on the loop can stall shutdown.
- sli-not-implemented (`app/core/telemetry.py`): The flow success/latency/validation/throughput/freshness SLIs are keyed on `_authenticated_user_key`, which reads `request.state.user`, but the POST /users registration and POST /users/login routes are UNauthenticated (no user set on request.state at that point), so `_flow_start_times` is never populated with a valid key and `flow.duration` never records — the E2E flow latency and freshness SLIs are silently unbacked.
- qr-metric-naming (`app/core/telemetry.py`): New metric name `http.server.requests` (plain count of completed requests) collides conceptually with semconv and is unsuffixed while representing a total; using it alongside `http.server.request.duration` is fine, but the singular/plural inconsistency (`http.server.requests` vs semconv `http.server.request`) mixes conventions in one diff.

</details>
**Outstanding (3):**
- ⛔ qr-preserves-behavior (`app/core/telemetry.py`:260): _worker_pool_busy_callback and telemetry_middleware reference _active_request_count, but the observable gauge and middleware are defined BEFORE the module-level `_active_request_count = 0` assignment. At import time the callback closure and function body only resolve the name at call time, so this is fine; however the `global _active_request_count` in the middleware combined with the later assignment ordering means the name binding is correct — no issue. — _No change needed._
- ⚠ qr-metric-naming (`app/core/telemetry.py`:100): New counter names use dot-as-namespace with a trailing '.count' segment (http.server.request.count, auth.attempts.count, flow.entry.count, flow.outcome.count, flow.validation.count), but per semconv style plain counts are unsuffixed — the '.count' suffix encodes the aggregation rather than what is measured. — _Drop the '.count' suffix (e.g. http.server.requests, auth.attempts, flow.entries) to match unsuffixed-count semconv style._
- ⚠ sli-fabricated-target (`app/core/telemetry.py`:210): Flow completion attributes distinguish steps via URL suffix matching on '/articles' POST as the terminal 'publish' event, which correctly maps to real routes in the repo, but flow duration correlation keys on the raw Authorization header shared across concurrent requests, so flow.duration will frequently mismatch entries to exits and record spurious durations. — _Correlate flow start/terminal by authenticated user identity rather than by request-scoped header/id keys that do not persist across the multi-request flow._

## SLIs implemented
- **Fastapi-realworld-example-app — HTTP Service Availability** (`fastapi-realworld-example-app-http-availability`) — target 99.95% over 30-day
- **Fastapi-realworld-example-app — HTTP Response Time P95** (`fastapi-realworld-example-app-http-latency-p95`) — target < 200ms over 1-hour
- **Fastapi-realworld-example-app — HTTP Response Time P99** (`fastapi-realworld-example-app-http-latency-p99`) — target < 750ms over 1-hour
- **Fastapi-realworld-example-app — HTTP 5xx Error Rate** (`fastapi-realworld-example-app-http-error-rate`) — target < 0.1% over 1-hour
- **Fastapi-realworld-example-app — HTTP Worker Pool Saturation** (`fastapi-realworld-example-app-http-saturation`) — target < 80% over 5-minute
- **Fastapi-realworld-example-app — Authentication Failure Rate** (`fastapi-realworld-example-app-http-auth-failure-rate`) — target < 2% over 1-hour
- **Fastapi-realworld-example-app — HTTP Request Throughput** (`fastapi-realworld-example-app-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `app/main.py`

## New files
- `app/core/telemetry.py`

## Dependencies
- `opentelemetry-api==1.44.0`
- `opentelemetry-sdk==1.44.0`
- `opentelemetry-exporter-otlp-proto-http==1.44.0`
- `opentelemetry-instrumentation-fastapi>=0.65b0`
<!-- houndos:entity fastapi-realworld-example-app end -->
