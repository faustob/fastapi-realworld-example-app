# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity fastapi-realworld-example-app start -->
## Fastapi-realworld-example-app

Generated by HoundOS against `029eb77`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · poetry · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ✓ clean — no issues found.
<details><summary>5 issues found and fixed during self-review</summary>

- qr-not-a-noop (`app/core/telemetry.py`): This telemetry module builds and registers the providers at import time, but nothing in the diff imports it or calls init_telemetry from the application factory (app.main.get_application). As written the module is never imported, so the SDK is never registered and every meter/tracer is a silent no-op.
- sli-not-implemented (`app/core/telemetry.py`): The auth-failure-rate SLI (fastapi-realworld-example-app-http-auth-failure-rate) defines auth_attempts_counter but nothing in the diff or repo records to it — no instrumentation of POST /api/users/login records auth outcomes/reasons, so the metric is unwired.
- sli-not-implemented (`app/core/telemetry.py`): The primary-flow SLIs (flow-success, flow-latency, validation-failure-rate, throughput, freshness) declare flow_entry_counter, flow_outcome_counter, flow_duration_histogram, flow_entry_to_terminal_histogram, and flow_validation_counter, but nothing in this diff or in existingHelperCallSites records to any of them. The flow instruments are never invoked from any code path.
- qr-unwired-telemetry (`app/core/telemetry.py`): http_active_requests (and the saturation SLI it backs) is only decremented/incremented in _saturation_middleware, which is registered via app.middleware("http") inside init_telemetry — but init_telemetry itself is never called from anywhere in the diff, so the middleware is never attached and the saturation instrument never records.
- qr-metric-naming (`app/core/telemetry.py`): New counter names use a '.total' / plural mix inconsistently: flow.entry.total is suffixed .total while sibling counters (flow.outcomes, auth.attempts, flow.validation.outcomes) are unsuffixed plurals, giving two conventions for plain counts in one diff.

</details>

## SLIs implemented
- **Fastapi-realworld-example-app — HTTP Service Availability** (`fastapi-realworld-example-app-http-availability`) — target 99.95% over 30-day
- **Fastapi-realworld-example-app — HTTP Response Time P95** (`fastapi-realworld-example-app-http-latency-p95`) — target < 200ms over 1-hour
- **Fastapi-realworld-example-app — HTTP Response Time P99** (`fastapi-realworld-example-app-http-latency-p99`) — target < 750ms over 1-hour
- **Fastapi-realworld-example-app — HTTP 5xx Error Rate** (`fastapi-realworld-example-app-http-error-rate`) — target < 0.1% over 1-hour
- **Fastapi-realworld-example-app — HTTP Worker Pool Saturation** (`fastapi-realworld-example-app-http-saturation`) — target < 80% over 5-minute
- **Fastapi-realworld-example-app — Authentication Failure Rate** (`fastapi-realworld-example-app-http-auth-failure-rate`) — target < 2% over 1-hour
- **Fastapi-realworld-example-app — HTTP Request Throughput** (`fastapi-realworld-example-app-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `app/main.py`
- `pyproject.toml`
- `app/api/routes/authentication.py`
- `app/api/dependencies/authentication.py`
- `app/api/errors/http_error.py`
- `app/api/errors/validation_error.py`

## New files
- `app/core/telemetry.py`

## Dependencies
- `opentelemetry-api`
- `opentelemetry-sdk`
- `opentelemetry-exporter-otlp`
- `opentelemetry-instrumentation-fastapi`
<!-- houndos:entity fastapi-realworld-example-app end -->
