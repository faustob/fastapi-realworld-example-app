# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity primary-flow-end-to-end start -->
## Primary Flow — End-to-End

Generated by HoundOS against `029eb77`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · poetry · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⚠ passed with warnings.
<details><summary>8 issues found and fixed during self-review</summary>

- qr-otel-version-pin (`pyproject.toml`): opentelemetry-api is pinned at 1.27.0, but this job targets the OTel 1.44 line (default-latest). Different PRs against one repo pinning different OTel lines makes the dependency move for no stated reason.
- dependency-missing-pin (`pyproject.toml`): The instrumentation imports opentelemetry-exporter-otlp-proto-grpc but it is not declared as a dependency (pyproject.toml). Installing from the manifest and running the app would fail with ModuleNotFoundError. Listing the packages in the PR description is NOT declaring them.
- sli-not-implemented (`app/api/routes/authentication.py`): The register() handler imports and calls flow_entries_total, flow_validation_outcomes_total and validation_step, but never opens a flow_outcome(...) context. Only the article-publish path records flow.outcomes and flow.duration, so the registration entry point of the primary flow contributes flow.entries but no terminal outcome/duration — the E2E flow success and latency SLIs measure only the publish hop, not the registration-to-first-article flow they claim.
- qr-preserves-behavior (`app/api/routes/articles/articles_resource.py`): create_new_article now returns ArticleInResponse from inside the `with flow_outcome(...)` block. Because the return happens inside the context manager, the return value is produced before the span/counter finalization in flow_outcome's finally block — this is correct, but the counter is recorded as "success" only when the block completes; any exception before return marks failure. This preserves behavior and is acceptable.
- qr-export-pipeline (`app/core/telemetry.py`): Providers are built with BatchSpanProcessor and PeriodicExportingMetricReader but neither the TracerProvider nor MeterProvider is ever shut down/flushed on process exit, so buffered spans and the final metric export can be lost when the process stops.
- sli-not-implemented (`app/core/telemetry.py`): The SLI 'Primary Flow — E2E Business Flow Throughput' is backed only by flow.outcomes/flow.entries. flow_entries_total (backing flow.entries) is imported and incremented ONLY in authentication.py's register handler; the article-publish path in articles_resource.py never records flow.entries, so throughput of the terminal publish step has no dedicated entry measurement — but this is a partial gap, see the concrete unwired issue below.
- qr-metric-naming (`app/api/routes/authentication.py`): flow_validation_outcomes_total.add records attribute key "outcome" while the schema-validation failure recorded in validation_error.py also uses "outcome"; but the per-step validation contextmanager records "validation.outcome" on the span vs "outcome" on the counter — inconsistent attribute keying across the same metric flow.validation.outcomes is acceptable, omit.
- qr-preserves-behavior (`app/api/routes/authentication.py`): In the register handler the return statement is now inside the `with flow_outcome("register"):` block, so a successful registration completes the block normally and records a success outcome; this preserves behavior and is correct. No issue.

</details>
**Outstanding (1):**
- ⚠ sli-not-implemented (`app/api/routes/authentication.py`:73): The login endpoint (a stated part of the primary registration->login->publish flow) records no flow telemetry; only register and create-article are instrumented. The E2E flow throughput/success SLIs are still backed by the register/publish measurement points, so this is a completeness gap rather than a total miss. — _Wrap the login handler in flow_outcome("login") and record flow_entries_total for the login step so the full E2E flow is measured._

## SLIs implemented
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour

## Modified files
- `app/main.py`
- `app/core/events.py`
- `app/api/routes/authentication.py`
- `app/api/routes/articles/articles_resource.py`
- `app/api/errors/validation_error.py`
- `pyproject.toml`

## New files
- `app/core/telemetry.py`

## Dependencies
- `opentelemetry-api>=1.44,<2.0`
- `opentelemetry-sdk>=1.44,<2.0`
- `opentelemetry-exporter-otlp-proto-grpc>=1.44,<2.0`
<!-- houndos:entity primary-flow-end-to-end end -->
